# 02 · 系统架构

## 1. 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 前端框架 | Next.js 14+ (React + TypeScript) | 全栈一体、SSE 流式支持好、部署简单(Vercel/自托管均可) |
| 卡片画布 | React Flow (@xyflow/react) + dagre/elkjs 自动布局 | 成熟的节点-连线画布方案,自定义节点渲染 Markdown 卡片 |
| 3D 思维宇宙 | react-force-graph-3d (Three.js) | 现成的 3D 力导向图,支持自定义节点材质与交互 |
| 状态管理 | Zustand + React Query | 画布本地状态轻量化;服务端状态缓存与乐观更新 |
| 后端 | Next.js API Routes(MVP)→ 独立 Node 服务(扩展期) | 单仓库起步,LLM 网关逻辑独立成模块便于日后拆分 |
| LLM | Claude API — `claude-opus-5` | 见 04 文档;`@anthropic-ai/sdk` 官方 TS SDK |
| Embedding | Voyage AI (`voyage-3`) | Anthropic 官方推荐;思维宇宙聚类与锚点检索 |
| 数据库 | PostgreSQL 16 + pgvector | 卡片树(关系型)与向量检索(锚点/去重)一库搞定 |
| 文件存储 | S3 兼容对象存储 + Anthropic Files API | PDF 原件存 S3;喂给模型用 Files API 免重复上传 |
| 鉴权 | NextAuth / Clerk | 标准方案,不自研 |

## 2. 整体架构图

```
┌─────────────────────────── 浏览器 ───────────────────────────┐
│  卡片画布 (React Flow)   PDF 阅读器 (pdf.js)   思维宇宙 (3D)  │
│         │  SSE 流式渲染 + 术语高亮解析([[term]])            │
└─────────┬────────────────────┬───────────────────┬──────────┘
          │ REST + SSE         │ REST               │ REST
┌─────────▼────────────────────▼───────────────────▼──────────┐
│                     API 层 (Next.js Routes)                  │
│   /api/cards/*        /api/documents/*      /api/universe/*  │
├──────────────────────────────────────────────────────────────┤
│                        服务层 (核心模块)                      │
│                                                              │
│  ┌────────────┐  ┌─────────────┐  ┌──────────────────────┐  │
│  │ CardService │  │ DocService  │  │  UniverseService     │  │
│  │ 树 CRUD     │  │ PDF 解析    │  │  评审/嵌入/图谱      │  │
│  │ 去重/跳转   │  │ 引用定位    │  │  锚点检索            │  │
│  └──────┬─────┘  └──────┬──────┘  └──────────┬───────────┘  │
│         │               │                    │               │
│  ┌──────▼───────────────▼────────────────────▼───────────┐  │
│  │              LLM Gateway(统一网关模块)                │  │
│  │  · Prompt 组装(上下文继承链)                          │  │
│  │  · 流式转发(Anthropic SSE → 客户端 SSE)               │  │
│  │  · Prompt 缓存断点管理    · 结构化输出 (parse)          │  │
│  │  · 重试/超时/refusal 处理  · 用量统计                   │  │
│  └──────┬──────────────────────────────┬──────────────────┘  │
└─────────┼──────────────────────────────┼─────────────────────┘
          │                              │
   ┌──────▼──────┐              ┌────────▼────────┐
   │ Claude API  │              │  Voyage AI      │
   │ claude-opus-5│              │  embeddings     │
   └─────────────┘              └─────────────────┘
          │
   ┌──────▼──────────────────────────────────────┐
   │ PostgreSQL (+pgvector)   S3   Files API      │
   └──────────────────────────────────────────────┘
```

## 3. 关键链路

### 3.1 生成子卡片(最高频链路)

```
用户点击术语「希尔伯特空间」
  → POST /api/cards  {parentId, sourceTerm, type: "child"}
  → CardService: 查同树是否已有同名卡片 → 有则返回 {redirect: cardId}
  → 无则建卡(status=generating),返回 cardId + SSE URL
  → 前端立即挂载空卡片,连接 GET /api/cards/{id}/stream
  → LLM Gateway:
      · 组装上下文:系统提示词(缓存) + 文献内容(缓存,若有)
        + 祖先链摘要(缓存) + 父卡片全文 + 本次指令
      · client.messages.stream(...) 流式调用
      · 逐 token 转发给前端;前端实时渲染 Markdown,
        解析 [[术语|一句话预览]] 标记为可点击高亮
  → 流结束:持久化全文、术语表、更新祖先链摘要缓存
```

延迟预算:首 token < 1.5s(靠 Prompt 缓存命中),整卡 5–15s(流式感知延迟≈0)。

### 3.2 思维宇宙入库

```
用户提交总结
  → POST /api/universe/reviews {cardId, summaryText}
  → LLM Gateway: messages.parse 结构化评审(见 04 文档 Schema)
  → 通过:
      · Voyage 生成 embedding → 存 pgvector
      · 后台任务:UMAP 增量降维 → 更新 3D 坐标
      · 返回评语 + 星体数据,前端播放"入宇宙"动画
  → 未通过:返回 {passed:false, gaps:[...], hint}
```

### 3.3 知识锚点注入

```
新建根卡片 / 新话题
  → UniverseService: 话题文本 embedding → pgvector 余弦检索 top-5 恒星
  → 相似度 > 0.55 的锚点注入生成 Prompt 的"个性化教学"段
  →(注入段位于缓存断点之后,不破坏前缀缓存)
```

## 4. 模块边界说明

- **LLM Gateway 是唯一碰 Anthropic SDK 的模块。** 所有 Prompt 模板、缓存断点、模型名、重试策略集中于此,业务模块只传领域参数。这保证未来换模型/调 Prompt 不扩散到业务代码。
- **CardService 拥有树结构的全部不变量**:父子关系、去重、祖先链摘要的生成与失效。
- **UniverseService** 独立拥有评审、嵌入、图谱三个子能力,MVP 期可整体降级关闭而不影响主链路。

## 5. 非功能性设计

| 关注点 | 方案 |
|---|---|
| 流式可靠性 | SSE 断线:卡片记录已生成的部分文本,前端重连后从 DB 取全文补齐(不重新生成) |
| 生成失败 | 卡片进入 error 态,提供"重试"按钮;LLM Gateway 对 429/5xx 依赖 SDK 自动重试(默认 2 次) |
| refusal 处理 | `stop_reason === "refusal"` 时卡片显示友好文案;网关默认开启 server-side fallbacks(见 04) |
| 成本控制 | Prompt 缓存(见 04)+ 每用户每日生成配额 + 用量埋点(usage 字段入库) |
| 并发生成 | 同一用户同时生成卡片数限 3,超出排队,防止误触雪崩 |
| 安全 | API Key 仅存服务端;用户内容按 userId 严格隔离;PDF 解析在服务端沙箱 |
