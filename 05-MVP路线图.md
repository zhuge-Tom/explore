# 05 · MVP 路线图

> **实现状态(2026-07-26)**:M1/M2/M3 的功能范围已全部在 `explore/` 中实现并构建通过,
> 另追加了导出 Markdown、重新生成、删除、配额与用量看板、性能优化等完善项(见 explore/README.md)。
> 尚未完成:真实 API Key 端到端验证(等待填 Key)、各阶段的**用户验收指标**(需真实使用数据)、
> 登录/多用户、切 Postgres。文中的验收标准仍然有效,作为填 Key 后的验证清单使用。

## 阶段划分总览

```
M1 核心闭环(4–6 周)      M2 文献与打磨(4 周)       M3 思维宇宙(4–6 周)
─────────────────────    ─────────────────────    ─────────────────────
卡片树 + 流式生成          PDF 导入 + 引用定位        总结评审
点击术语派生子卡片          关联/分支卡片              3D 宇宙 + 锚点教学
自动布局 + 面包屑          去重跳转、折叠、小地图      (v2) 遗忘复习
```

## M1 — 核心闭环:「点击术语 → 长出卡片」

**目标:验证层级对话这一交互本身是否成立。**

范围:

- [ ] 登录、新建树(纯文本问题)
- [ ] 根卡片流式生成,`[[术语|预览]]` 高亮渲染
- [ ] 点击术语 → 子卡片(唯一卡片类型)+ 连线 + dagre 自动布局
- [ ] 面包屑、画布缩放/平移
- [ ] LLM Gateway:系统提示词缓存、祖先链摘要、refusal/错误态、usage 落库
- [ ] 每用户日配额

**验收标准**:5 名种子用户完成一次 ≥ 3 层深挖;术语点击率 ≥ 1 次/卡片;首 token P90 < 2s;缓存命中率(深挖场景 cache_read > 0 的请求占比)> 70%。

明确不做:文献、关联/分支卡片、思维宇宙、移动端。

## M2 — 文献模式与体验打磨

**目标:接住"读论文"这一最强付费场景。**

范围:

- [ ] PDF 上传 → Files API → 双栏模式(pdf.js + 画布)
- [ ] 划词/点击生成卡片,回答带 citations,角标跳回原文
- [ ] 关联卡片(横向对比)、分支卡片(另起炉灶)
- [ ] 同名术语去重跳转、子树折叠、小地图、聚焦模式
- [ ] 断线重连补齐、卡片重试

**验收标准**:文献树的平均卡片数 ≥ 8;引用点击率 > 30%;NPS 访谈确认"比在聊天框里读论文强"。

## M3 — 思维宇宙

**目标:建立留存与数据壁垒。**

范围:

- [ ] 「我来总结」入口 + 结构化评审(通过/未通过 + 引导)
- [ ] 恒星入库:Voyage embedding + UMAP 3D 坐标 + 入宇宙动画
- [ ] 3D 宇宙页(react-force-graph-3d):恒星、连线、点击回看
- [ ] 知识锚点注入生成 Prompt
- [ ] 评审漏斗埋点(提交率/通过率/修改重交率)

**验收标准**:总结提交率 ≥ 20%(有卡片的用户);通过率在 40–70% 区间(过高说明评审放水,过低劝退);7 日留存相对 M2 提升可测。

v2 候选(数据说话后再排):遗忘曲线与复习卡片、树分享/公开画廊、多文献交叉树、移动端阅读态。

## 风险与对策

| 风险 | 判断 | 对策 |
|---|---|---|
| 术语高亮质量差(标记过多/过少/不当) | 中高 | M1 期人工评审 100 张卡片调 Prompt;高亮数量硬约束写进系统提示词;埋点术语点击率按术语聚合 |
| 成本失控(深树 + 长文献) | 中 | 缓存分层设计(04 文档)+ 配额 + usage 实时监控;文献超长时分段(只缓存当前阅读区段) |
| 评审"太严劝退 / 太松无意义" | 中 | 通过率作为核心健康指标持续调参;completeness 不卡通过的规则先行 |
| 画布性能(500+ 卡片) | 低(MVP 期) | React Flow 虚拟化 + 折叠;单树卡片数软上限 300 |
| 3D 宇宙沦为花瓶 | 中 | 锚点教学是宇宙的"功能性出口",M3 必须同期上线,不做纯展示版 |
