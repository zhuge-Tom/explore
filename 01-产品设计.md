# 01 · 产品设计

## 1. 目标用户与场景

| 用户画像 | 典型场景 |
|---|---|
| 研究生 / 科研人员 | 读论文时遇到陌生方法、术语,需要边读边深挖 |
| 跨领域学习者 | 自学生物 / 量子力学 / 机器学习等术语密集型学科 |
| 知识工作者 | 快速进入一个新领域,需要建立结构化认知而非碎片问答 |

共同特征:**问题不是一次性的,而是树状展开的**;学习目标是"建立体系",不是"得到一个答案"。

## 2. 用户旅程(主线)

```
输入一个问题 / 导入一篇文献
        │
        ▼
根卡片生成(流式输出,术语高亮)
        │
        ▼
点击不懂的术语 ──────────► 子卡片在右侧丝滑展开
        │                       │
        ▼                       ▼
继续点击 / 创建关联卡片      无限向下钻取,树自动布局
        │
        ▼
对某张卡片"我来总结一下" ──► AI 评审 ──► 通过 ──► 存入思维宇宙 ✨
                                │
                                └─ 未通过 ──► AI 指出理解偏差,引导修正
```

## 3. 卡片系统(核心交互)

### 3.1 卡片解剖

每张卡片包含:

- **标题**:术语 / 问题本身;
- **正文**:AI 生成的 Markdown 内容,其中的关键术语渲染为**可点击高亮**(点击即生成子卡片);
- **面包屑**:从根卡片到当前卡片的路径(如 `量子力学 › 波函数 › 希尔伯特空间`),保证"主线始终清晰可见";
- **操作区**:✍️ 我来总结(进入思维宇宙流程)、➡️ 创建关联卡片、⬇️ 创建分支卡片、📌 收藏、🗑 删除。

### 3.2 三种派生卡片

| 类型 | 手势 | 上下文继承 | 用途 | 示例 |
|---|---|---|---|---|
| ↗️ 子卡片 | 点击正文中的术语 | 继承祖先链摘要 + 父卡片全文 | 深挖背景知识 | 在「波函数」卡片里点击「希尔伯特空间」 |
| ➡️ 关联卡片 | 选中术语后选择"对比/关联" | 继承祖先链摘要 + 父卡片全文,提示词要求横向对比 | 横向发散、辨析易混概念 | 「波函数坍缩」vs「退相干」 |
| ⬇️ 分支卡片 | 卡片底部"另起炉灶" | 继承完整上下文,但用户输入全新问题 | 换个角度追问,不污染原分支 | 学完数学形式后,问"这在实验上怎么验证?" |

**设计原则:上下文继承是自动的、不可见的。** 用户永远不需要复述"我们刚才聊到哪了"——这正是对聊天框的降维打击。

### 3.3 画布与布局

- 无限画布,卡片树**从左向右**生长(根在左,便于阅读方向一致);
- 新卡片展开动画:从点击的术语位置"生长"出来,连线动画 200ms;
- 自动布局(dagre/elk 层次布局)+ 允许手动拖拽微调;
- **小地图 + 聚焦模式**:双击卡片进入聚焦(当前卡片 + 面包屑放大居中),Esc 返回全景;
- 折叠:任意节点可折叠其子树,折叠后显示计数徽标(`+7`)。

### 3.4 术语高亮的产品规则

- 每张卡片高亮 **3–8 个**术语(太多 = 认知过载,太少 = 无处可点);
- 已经在树中存在同名卡片的术语,点击时**跳转**到已有卡片并高亮连线,而不是重复生成(知识树去重);
- 悬停术语显示一句话预览(生成卡片时一并返回),降低"点开才知道值不值得看"的成本。

## 4. 文献导入

- 支持 PDF 上传(v1)、URL / arXiv 链接(v1.5);
- 导入后进入**双栏模式**:左侧原文阅读器,右侧卡片画布;
- 在原文中**划词或点击**,即以"文献上下文 + 选中内容"为根,生成卡片;
- 卡片回答必须**带引用**:标注来自原文第几页哪一段(利用 Claude Citations 能力),点击引用跳回原文位置;
- 一篇文献对应一棵(或多棵)知识树,树与文献永久关联。

## 5. 思维宇宙(留存与激励闭环)

### 5.1 流程

1. 用户在任意卡片点击「✍️ 我来总结」,用自己的话写下理解(建议 50–300 字);
2. AI 评审(见 04 文档的评分 Prompt):从**准确性、完整性、是否用了自己的话**三个维度打分;
3. **通过** → 该理解成为一颗"恒星"存入思维宇宙,卡片打上 ⭐ 已内化标记;
4. **未通过** → AI 温和指出具体偏差(不给答案,给线索),用户可修改重交;连续两次未过,AI 给出示范性对比。

### 5.2 3D 呈现

- 每颗恒星 = 一条通过评审的理解,位置由其文本 embedding 降维(UMAP → 3D)决定:**语义相近的理解在空间上自然聚团**;
- 恒星之间的连线 = 知识树上的父子/关联关系 + 语义相似度超阈值的暗线;
- 星等(大小/亮度)= 该概念在树中的连接度 + 复习巩固次数;
- 支持点击恒星回看:当时的卡片、自己的总结、AI 评语。

### 5.3 知识锚点教学(长期壁垒)

当用户开启新话题时,系统检索思维宇宙中语义最近的 3–5 颗恒星,注入生成 Prompt:

> "该用户已经用自己的话理解了『傅里叶变换是换一组基来看信号』,请在解释量子态的基展开时,类比他的这条理解。"

**效果:AI 用"你自己说过的话"教你新东西** —— 这是任何通用聊天产品都给不了的个性化体验,也是数据护城河。

### 5.4 遗忘与复习(v2)

- 恒星随时间"变暗"(艾宾浩斯曲线);
- 变暗的恒星触发复习卡片:AI 就该概念出一个新情境的问题,答对则恒星重新变亮。

## 6. 关键产品指标

| 指标 | 意义 |
|---|---|
| 平均树深度 / 树广度 | 层级对话是否真的被使用(目标:平均深度 ≥ 3) |
| 术语点击率 | 高亮质量(目标:每卡片 ≥ 1 次点击) |
| 总结提交率 & 通过率 | 思维宇宙漏斗健康度 |
| 7 日回访 + 宇宙回看率 | 沉淀是否形成留存 |

## 7. 竞品差异

| | ChatGPT/Claude 聊天框 | Obsidian/Roam | Explore |
|---|---|---|---|
| 结构化追问 | ❌ 线性淹没 | ❌ 手动整理 | ✅ 点击即分叉,自动成树 |
| 上下文继承 | ⚠️ 单线程 | ❌ | ✅ 按分支精确继承 |
| 学习验证 | ❌ | ❌ | ✅ AI 评审 + 思维宇宙 |
| 个性化教学 | ⚠️ 泛化记忆 | ❌ | ✅ 用用户自己的理解做锚点 |
