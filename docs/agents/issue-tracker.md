# Issue tracker

This project uses GitHub Issues in `zhuge-Tom/explore`.

- Read and create issues with the GitHub CLI (`gh`).
- Link implementation work to the relevant issue when one exists.
- Do not create local issue files under `.scratch/`.
