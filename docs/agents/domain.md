# Domain documentation

The repository uses a single-context layout.

- Read `CONTEXT.md` at the repository root for domain language when present.
- Read architecture decisions from `docs/adr/` when present.
- Update these documents when implementation changes domain terminology or establishes a lasting architectural decision.
